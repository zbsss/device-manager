apiVersion: v1
kind: Pod
metadata:
  name: device
spec:
  containers:
  - name: device
    image: docker.io/zbsss/device:latest
    imagePullPolicy: Always
    env:
      - name: MY_POD_IP
        valueFrom:
          fieldRef:
            fieldPath: status.podIP
